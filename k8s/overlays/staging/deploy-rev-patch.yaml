# Auto-generated by .github/workflows/build-deploy.yml on each
# main-branch push — the deploy-rev annotation changes with every VERSION
# so Flux rolls the pod (picking up the latest :latest image + the current
# platform-version ConfigMap value); the insula.host/version label makes the
# running version queryable on the Deployment object itself.
# Do not edit by hand — edits will be overwritten.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-api
  namespace: platform
  labels:
    insula.host/version: "2026.6.2-b71de89"
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.2-b71de89"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-panel
  namespace: platform
  labels:
    insula.host/version: "2026.6.2-b71de89"
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.2-b71de89"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-panel
  namespace: platform
  labels:
    insula.host/version: "2026.6.2-b71de89"
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.2-b71de89"
---
# firewall-reconciler — :latest-tagged DaemonSet that needs
# rolling on every push so the new image SHA is picked up
# (imagePullPolicy: Always on the container).
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: firewall-reconciler
  namespace: platform-system
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.2-b71de89"
---
# mail-stack-standby-replicate — :latest-tagged DaemonSet
# (mail-backup-tools image) — same rationale as above. New
# script changes in images/mail-backup-tools/ are baked into
# :latest by ci-mail-backup-tools.yml; this annotation
# ensures the running Pods restart to pick them up.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mail-stack-standby-replicate
  namespace: mail
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.2-b71de89"
