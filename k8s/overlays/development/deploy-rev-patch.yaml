# Auto-generated by .github/workflows/build-deploy.yml on each
# main-branch push — the deploy-rev annotation changes with every VERSION
# so Flux rolls the pod (picking up the current platform-version ConfigMap
# value); the insula.host/version label makes the running version queryable
# on the Deployment object itself.
# Do not edit by hand — edits will be overwritten.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-api
  namespace: platform
  labels:
    insula.host/version: "2026.6.8-f5411ae"
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.8-f5411ae"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-panel
  namespace: platform
  labels:
    insula.host/version: "2026.6.8-f5411ae"
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.8-f5411ae"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-panel
  namespace: platform
  labels:
    insula.host/version: "2026.6.8-f5411ae"
spec:
  template:
    metadata:
      annotations:
        insula.host/deploy-rev: "2026.6.8-f5411ae"
