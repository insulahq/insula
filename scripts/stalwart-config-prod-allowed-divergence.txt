# Sections allowed to diverge between base stalwart-config.toml and
# production config-overrides.toml.
#
# Add a TOML section header here (e.g. [certificate.default]) to suppress
# the ci-stalwart-config-drift.sh failure for that section.
#
# Production-only additions (present in prod, absent from base):
[certificate.default]

# Sections whose values legitimately differ in production vs base:
# (store.pg.tls — production enables TLS; base has enable=false)
[store.pg.tls]
