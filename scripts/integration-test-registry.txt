# integration-test-registry.txt — the single source of truth for every
# scripts/integration-*.sh. Enforced by scripts/ci-integration-coverage.sh:
# a new integration-*.sh that is not listed here FAILS CI (no silent orphans).
#
# Format:  <category>  <script-basename>  # <note>
# Categories:
#   orchestrator  - the suite runners themselves
#   suite         - wired into integration-all.sh (guard verifies the reference)
#   suite-staging - invoked by integration-staging.sh
#   helper        - sourced/called by a wired script (not a standalone suite)
#   perf          - performance benchmark, on-demand only (never CI-gated)
#   local         - local-DinD/docker harness (docker exec); NOT the SSH staging suite
#   manual        - maintained standalone harness, run by hand (heavy/seed-chain root)
#   pending       - triaged for integration but NOT yet wired; <- the backlog.
#                   Note carries the target array/tier + any fix needed (ADR-053 §itest-cleanup).
# Retired scripts are deleted, not listed (see the tombstone block at the end).

# ── runners ───────────────────────────────────────────────────────────────
orchestrator  integration-all.sh
orchestrator  integration-staging.sh

# ── wired into integration-all.sh ────────────────────────────────────────
suite         integration-token.sh                       # shared api_curl/login helper
suite         integration-cleanup.sh                     # SERIAL_PRE fixture cleanup
suite         integration-pvc.sh
suite         integration-grow-e2e.sh
suite         integration-oidc-dex.sh
suite         integration-drain-e2e.sh
suite         integration-passkey-e2e.sh
suite         integration-firewall-e2e.sh
suite         integration-waf-crowdsec.sh
suite         integration-dr-drill-shim.sh
suite         integration-lifecycle-e2e.sh
suite         integration-node-terminal.sh
suite         integration-postgres-pitr.sh              # default (snapshot-LSN) mode wired into integration-all + STAGING-validated. WITH_WAL=1 mode (folded 2026-07-02 from the retired snapshot-restore.sh) = recoveryTargetTime WAL-replay (marker-A survives/marker-B gone + slow-path forced); on-demand only, needs a barman-archiving cluster (staging), NOT DEV (empty spec.plugins). Also folds chip-persistence + plugin-sidecar-propagation assertions (run in both modes).
suite         integration-tier-flip-e2e.sh
suite         integration-monitoring-slo.sh
suite         integration-system-snapshots.sh
suite         integration-mailbox-quota-e2e.sh
suite         integration-backup-rclone-shim.sh
suite         integration-cluster-trusted-proxies.sh
suite         integration-wal-archive-failure-e2e.sh
suite         integration-waf-failure-e2e.sh
suite         integration-webmail-feature-toggle.sh     # SERIAL_PRE/core; rolls webmail deploys; DEV-validated 2026-06-30

# ── invoked by integration-staging.sh ────────────────────────────────────
suite-staging integration-stalwart-mail-ha.sh            # shared mail-TLS/SMTP probe helpers
suite-staging integration-system-wal-archive.sh
suite-staging integration-platform-ops-cli-e2e.sh
suite-staging integration-system-backup-pg-dump.sh

# ── helpers / chain sub-scripts (run via a maintained parent) ─────────────
helper        integration-tenant-bundles-roundtrip.sh    # called by files-browse-restore-e2e
helper        integration-tenant-bundles-tenant-side.sh  # called by roundtrip

# ── maintained standalone (run by hand: heavy or seed-chain root) ─────────
manual        integration-tenant-bundles-files-browse-restore-e2e.sh  # chain root
manual        integration-tenant-bundles-jmap-full-e2e.sh             # canonical jmap bundle E2E
manual        integration-tenant-bundles-mailbox-engine.sh            # L1 seed harness
manual        integration-bundle-coverage.sh
manual        integration-backups-ui.sh
manual        integration-catalog-local.sh
manual        integration-private-worker.sh
manual        integration-custom-deployments.sh                       # ADR-036 phase-1
manual        integration-system-dr-drill.sh                          # full cold-restore, dedicated VM
manual        integration-firewall-blacklist.sh                       # DEV-validated 2026-06-30; SAFE (bans reserved 203.0.113.222, self-lockout 422-guarded, trap-unban) — kept on-demand per operator firewall caution, not auto-run

# ── performance benchmarks (on-demand, never CI-gated) ────────────────────
perf          integration-shim-perf.sh                   # R-X14 rclone-shim throughput floors
perf          integration-stalwart-export-perf.sh        # DESTRUCTIVE (scale-0 + DataStore wipe)
perf          integration-stalwart-storage-perf.sh       # local-path vs Longhorn fio

# ── local-DinD / local-docker (docker exec; not the SSH staging suite) ────
local         integration-notifications.sh               # docker exec into k3s container
local         integration-system-tenant-e2e.sh           # local-DinD; FIX: bulk-delete route (see pending note)
local         integration-sftp-gateway.sh                # local-docker sftp-chroot contract
local         integration-secrets-bundle.sh              # local-DinD secrets-audit smoke
local         integration-tenant-backup-tools-dind.sh    # image build/binary smoke
local         integration-stalwart-v016-local.sh         # Stalwart 0.16 JMAP smoke (drop 8b/8c removed-route steps)

# ── pending integration (triaged 2026-06-30; backlog phase 2) ─────────────
# Each carries its target. FIX = code fix needed before wiring (route bit-rot etc).
manual        integration-mail-external-reachability.sh  # External mail-port reachability across all 3 exposure modes (probes 25/465/587/143/993/4190 from the workstation = external-customer view) + verifies exposure follows the mode. STAGING-VALIDATED GREEN 2026-07-02: "matches expected topology in ALL THREE MODES" (allServerNodes; activeNodeOnly=only active node serves; assignedMailNodes refusal when active∉set + success when in-set), deliverability 27 ok/0 fail, mode RESTORED to allServerNodes. Fix (b): api_patch/health minted a token in-pod then curled the platform-api CLUSTERIP FROM THE NODE — Calico blocks node-host→ClusterIP (curl(28) ~134s) so toggles never fired; routed both to `kubectl exec $AP -- env … node -e fetch(127.0.0.1:3000)` (in-pod, mirrors the token-mint + the mail-mobility fix) + made $AP pick a RUNNING pod (--field-selector; else it grabbed the Succeeded pitr-system-db Job pod that shares app=platform-api after a barman restore). DISRUPTIVE (toggles mail exposure) → run by hand on a quiescent cluster; restores the prior mode at the end. externalIP convergence can lag (retries).
manual        integration-mail-dr-failover.sh            # DESTRUCTIVE live DR failover E2E (stops k3s on the ACTIVE mail node → dr-watcher drives a real restore-based failover to the standby, ~3-5min with threshold=60). STAGING-VALIDATED GREEN 2026-07-02 (rc.2): stopped k3s on staging1 → dr_state healthy→degraded→failing-over after 60s → failover migration launched → "PASS: DR failover moved stalwart staging1→staging3"; trap restarted k3s; failed back to staging1 + mail health all-green (pod/jmap/rocksdb/cert/tcp/deliverability). Node SSH is now PARAMETERIZED (the old `.example.test` hardcode fixed): pass MAIL_DR_NODE_MAP="staging1=<ip>,staging2=<ip>,staging3=<ip>" (+ optional MAIL_DR_BASTION_NODE, MAIL_DR_NODE_SUFFIX). SETUP before a run: (1) PATCH /admin/mail/placement {primaryNode,secondaryNode:<standby>,autoFailoverEnabled:true,failoverThresholdSeconds:60} — dr-watcher EXITS if autoFailover=false (dr-watcher.ts:78), targets mailSecondaryNode; (2) label the standby `kubectl label node <standby> insula.host/mail-standby=true` (the placement PATCH does NOT apply it, but the suite's precondition greps for it); (3) ensure the dr-watcher (platform-api) is NOT on the node you stop, and a fresh mail snapshot exists (failover restores from the latest stalwart-snapshot CronJob run). AFTER: POST /admin/mail/failback {confirm:true}, then restore placement (secondary:null, autoFailover:false, threshold:300) + remove the standby label. SAFE re DB when system-db primary is off the stopped node. -> deliberate maintenance-window run, never CI-gated. DATA-PLANE FINDINGS (2026-07-02 destructive dp probe, staging): the suite is CONTROL-PLANE only (asserts migration launched/done + pod relocated) — it does NOT probe mail reachability or data integrity. Measured behavior beyond it: (a) REACHABILITY IS NOT CONTINUOUS — surviving nodes' mail ports stay bound but the HAProxy backend (active Stalwart) is down during relocation → ~2-3min unreachable window (measured staging2 ~161s / staging3 ~115s on :25); inbound SMTP senders RETRY so no loss, interactive IMAP/submission see the outage. (b) DATA: anything in the last snapshot/standby-rsync SURVIVES failover AND round-trips back on failback (verified: an SMTP msg delivered while on the standby returned to primary) — but RPO = snapshot age: a msg written AFTER the last snapshot and not yet rsync'd is LOST. (c) MASTER-USER AUTH (impersonation) 401s TRANSIENTLY after a restore-based failover (credential drift; auto-heals ≤5min) — breaks webmail-impersonation + backup tooling + any master-proxy verification in that window (a naive data-check gets FALSE NEGATIVES — wait for auth-heal + retry). (d) FAILBACK is scale-down-timeout FLAKY ("Deployment stalwart-mail did not reach 0 within 60s"; retry succeeds) and silently no-ops if the target node isn't Ready yet. A hardened data-plane harness must wait-for-master-auth-heal, retry reads, and treat the reachability gap as a reported metric. -> that harness now EXISTS: integration-mail-dr-dataplane.sh.
manual        integration-mail-dr-dataplane.sh           # DATA-PLANE companion to mail-dr-failover: proves the USER-VISIBLE properties the control-plane suite masks. Self-contained (seeds its own probe tenant/mailbox + tears down incl. failback + config restore). Asserts: D=snapshotted msg survives failover, I=inbound SMTP :25 to the new active lands, S=msgs written on the standby round-trip on failback, + reachability RESTORED post-failover; REPORTS (not FAIL) the reachability gap (~2-3min) + RPO (post-snapshot msg). HARDENED against the 3 false-negative causes: wait-for-master-auth-heal (401 credential-drift window post-restore), retry reads, failback target-Ready gate + retry. Env: MAIL_DR_NODE_MAP="name=ip,..." required (auto-detects TOOLS_IMAGE + standby; bastion auto-picks a surviving node != active; refresh_mp re-reads mail-secrets each attempt since Step 8c rotates the master). DESTRUCTIVE ~12-15min. STATUS 2026-07-04: FAILOVER half GREEN (rc.6: D=survived + I=inbound both PASS). The platform DR gaps the 2026-07-02/03 runs surfaced are FIXED (on development, reach staging via RC): (1) cert-serving self-heal + bounded served-cert probe (Step 8b3); (2) the DOMINANT gap — a restore reset BOTH admin+master creds to the snapshot, 401ing the domain-reconciler so listeners never self-healed — FIXED by re-syncing the admin (8b1) AND master (8b1b) credential onto Stalwart AT cutover (immediate), + mail-drift auto-heal; (3) snapshot integrity: snapshot-upload.sh skips an empty/mid-swap store (no RocksDB CURRENT) and restore-state walks candidate snapshots newest->oldest skipping empty ones, so a 0-byte DR-window snapshot can no longer poison `latest`; (4) FAILBACK reliability (was: hangs at scaling-up) = TARGET-NODE-READINESS GATE (preflight waits for the target Ready + not-ready/unreachable taints cleared BEFORE the destructive swap — a failback fires while the target is still recovering from its k3s restart) + STALE-LOCAL-PATH-PROVISIONER BOUNCE (bounce the provisioner pod when the fresh PVC will not bind; proven to bind in ~25s) + captureScaleUpDiagnostics (records pvc/pod/taint/events into the run error — this is what PINNED the failback cause). FAILBACK half GREEN 2026-07-04 (rc.9, run validate4: 12/0 — failover staging1->staging2 + D/I + failback relocated staging2->staging1 + sync-back + VALID cert on the reactivated primary, all PASS). Needed a HARNESS fix: the suite restarted the stopped primary's k3s only in the teardown trap, so every failback targeted a still-DOWN node — the platform's new preflight node-readiness gate correctly REFUSED it ('target node did not become schedulable within 300s'), exposing the gap; the failback now restarts the primary's k3s first. Gate VALIDATED BOTH WAYS: refuses a failback to a not-yet-Ready target (data-safe — fails at preflight, source intact) AND passes cleanly once the target is recovered (the composed provisioner-bounce is a no-op when the harness's settle gives the provisioner time). SETUP is flaky right after a mail migrate (email-domain enable 'no id' if Stalwart/mail-admin hasn't settled) — let mail settle before a run. ALSO FIXED 2026-07-03: restoreMailOnSource rollback PVC now matches the retained-PV capacity (was hardcoded 1Gi -> wedged Flux). NON-HA CAVEAT (staging: CNPG instances=1, platform-api replicas=1): a failover only launches if mail is NOT co-located with the DB node or the platform-api node (else stopping the mail node kills the control plane) — place mail on neither before a run. Prior repeated staging mail incidents -> run ONLY on an isolated/quiescent env.
manual        integration-mail-mobility-e2e.sh           # Mail port-exposure-mode toggle (allServerNodes<->activeNodeOnly) + DR failover-migration + snapshot-follows-active. DISRUPTIVE (toggles mail node exposure, drives a failover) — run by hand on a quiescent cluster, not auto-wired. Staging-validated fix landed 2026-07-01 (from 0fc4c069, previously stranded): activeNodeOnly enum rename, ClusterIP->public-admin-ingress transport (on-node Calico can't reach ClusterIP), deterministic snapshot triggers, retention restore.
manual        integration-mailbox-aux-orchestrator-e2e.sh # Orchestrator-level tenant-bundle mailbox capture+restore E2E: spawns capture+restore Jobs in `mail` ns from the real tenant-backup-tools image, running the EXACT shell the platform-api tenant-bundle orchestrator generates (catches script↔wrapper drift the L1 mailbox-engine can't). Asserts the tar has Maildir cur/ + .aux/{manifest,sieve,contacts,calendar,vacation,filenode}.json and both restore scripts exit clean. STAGING-VALIDATED GREEN 2026-07-02 (rc.2): "full orchestrator E2E PASS" (7/7 tar asserts + restore imported 1 msg + all 5 aux surfaces failed=none). The old "needs the whole export-perf benchmark env" was WRONG — a MINIMAL seed suffices (aux "available" is capability-based, not data-based; a fresh Stalwart user exposes all 5): (1) platform API tenant→domain(mailperf-bench.net)→enable-email→2 mailboxes e2e-src@/e2e-imap-dst@; (2) deliver ≥1 email to e2e-src via master-proxy JMAP import (auth user `<addr>%master@local.host`, pw = mail-secrets STALWART_MASTER_PASSWORD, hit in-cluster stalwart-mgmt:8080 /.well-known/jmap then rewrite apiUrl/uploadUrl host→that ClusterIP since the public URL is self-signed); (3) on the NODE write /tmp/mpw (=STALWART_MASTER_PASSWORD) + /tmp/mfqdn (=`master@local.host`); (4) run on-node with TOOLS_IMAGE=<containerd tag, NOT :latest → else ImagePullBackOff> + IMAGE_PULL_POLICY=IfNotPresent. Accounts now env-overridable (AUX_SRC_ADDRESS/AUX_DST_ADDRESS). -> manual seed-chain E2E, run by hand on-node; DISRUPTIVE-free but needs the seed + node kubectl.
manual        integration-master-user-rotation.sh        # DELIBERATE-RUN ONLY, never auto-wired: it TAMPERS+ROTATES the webmail master (a mail-secrets op) — a mid-run failure between tamper and restore breaks ALL mail, so run it by hand on a quiescent cluster, never in a parallel suite. Code fixes done 2026-07-01 (rollout-restart->pod-delete #142; cookie->Bearer; ADMIN_HOST de-hardcode; §4 accountId d333333 advisory). NOTE: the mail-drift auto-heal (2026-07-01) now re-asserts a drifted master within 5 min, so a failed rotation self-recovers — but still run deliberately.
suite         integration-bulwark-impersonate.sh         # Bulwark native /api/auth/impersonate handler (A pod-health, B happy-path incl. #296 JMAP read, C 14 negative JWT cases). Wired into integration-all PARALLEL. STAGING-VALIDATED 26/0 (2026-07-01, after the master-credential fix — B3 now reads 5 folders). In-pod via kubectl; self-skips(77) if bulwark scaled to 0.
suite         integration-webmail-platform-e2e.sh        # platform-driven webmail E2E incl. master-user impersonation SPA JMAP probe. Wired into integration-all PARALLEL. STAGING-VALIDATED 15/0 (2026-07-01, phase 11.1 "Stalwart accepted the cookie" after the master-credential fix). Orchestrator supplies API_BASE/ADMIN creds via integration.env.
suite         integration-dr-bundle.sh                   # System DR bundle export→download→decrypt (external-tier). Wired into integration-all PARALLEL. Phase A (export+download) STAGING-VALIDATED 2026-07-01 (202KB bundle); DEV-validated 18-pass decrypt/sidecars/E2-freeze/secrets. Self-skips(77) when age binary / AGE_KEY_FILE absent (public clone/CI); Phase G (psql) + H (tsx) self-skip too.
manual        integration-dr-protocols.sh                # Offline etcd break-glass READ path (restore-etcd-from-shim.sh --offline --list) over S3/SFTP/CIFS. NON-destructive (never restores etcd). DEV-VALIDATED 2026-07-02: S3 2/0 (offline restore resolved descriptor + connected + listed marker from the real store, no kubectl); SFTP/CIFS self-skip without creds. Runs node-side (needs rclone+python3 on the node); creds via env from servers.txt.
manual        integration-postgres-barman-restore.sh     # Destructive off-cluster barman restore + Phase-3.1 promote (WIPES+rebuilds source system-db). STAGING-VALIDATED 2026-07-02: catalogue(9 backups)→side-by-side restore healthy→bootstrapped-from-barman (post-archive marker ABSENT)→promote Job succeeded ~480s→source healthy→side-by-side auto-deleted (no orphan). Fixed a set-e cleanup bug (kubectl get cluster exits 1 on the NotFound SUCCESS case → aborted the script post-promote; +2 defensive || true on chip/lock psql reads). Run by hand on staging (~15-25min, destructive; production-paranoia guard). Not re-run after the fix to avoid a 2nd destructive wipe — core validated + staging verified healthy/no-orphan externally.
suite         integration-system-backup.sh               # System Backup Phase 1: secrets export -> one-shot signed URL (sha256 match) -> 410-Gone single-use -> fresh export new runId -> audit -> route smoke. Wired into integration-all PARALLEL. STAGING-VALIDATED 2026-07-01 (steps 1-6,8-10 green). age-decrypt content check (step 7) is external-tier -> soft-skips when AGE_KEY != operator recipient (bundle bytes already proven in step 5).
suite         integration-tenant-bundles-engine-e2e.sh   # MODE=api-smoke (mailbox_backup_engine PATCH+GET round-trip) wired into integration-all SERIAL_PRE. STAGING-VALIDATED 2026-07-01. Now takes MODE as env OR positional arg; api-smoke saves+restores the original engine (non-mutating). Heavy mailboxes-only/full modes (real S3 bundle) stay manual/on-demand.
manual        integration-tenant-bundles-restic.sh       # External-tier (needs ~/servers.txt S3 creds + a local restic via SPIKE_RESTIC + runner->S3 OBJECT egress). Writer-pod blocker FIXED 2026-07-02: POST /tenants/:id/files/start starts the on-demand file-manager (same fix as sftp-gateway-e2e; the [4/9] blocker). STAGING-VALIDATED core GREEN: provision->files/start->writer pod->seed->bundle status=completed (~13s). Step 8 (local restic read of the S3 repo) is bounded by a timeout and SOFT-SKIPS external-tier when the runner can't reach S3 object ops (this workstation: curl reaches the endpoint host but restic bucket ops stall) — the bundle-completed HARD gate already proves the platform wrote the repo. Run by hand from an S3-egress host for the full local round-trip. Prior 4 bit-rots also fixed 2026-07-01 (servers.txt ~/; TESTING_HOST->SSH_HOST; KUBECONFIG on inline-SSH kubectl; secret key platform-encryption-key).
suite         integration-burstable-qos.sh               # ADR-037 asymmetric QoS (quota shape, multi-component alloc, burstable-CPU/guaranteed-mem, over-alloc rejected at pod-admission). Wired into integration-all PARALLEL. STAGING-VALIDATED 10/0 (2026-07-01): assert-5 rewritten to poll the FailedCreate "exceeded quota" event (async pod-admission), NOT a sync 4xx; CONTROL_HOST falls back STAGING_SSH_HOST->SSH_HOST (orchestrator).
manual        integration-custom-deployments-phase2.sh   # ADR-036 phase-2 (chains after phase-1). RESOLVED 2026-07-02 (rc.2): the ADR-036 custom-deployments flow is PROVEN working on staging — verified 3× via controlled E2E repro (seed active tenant + POST simple/compose; web+api Running in 20-43s, wait-api initContainer Exit 0, api Service endpoint healthy, 2 svcs). Run recipe: run-customdep-staging shim (seed a tenant + KUBECTL=kubectl bare + the run-suite kubectl shim; the earlier empty-TENANT_NS abort was DEV-ONLY). Two prior "failures" were fixed IN phase-1 (integration-custom-deployments.sh): (1) PSS assert is now CONFIG-AWARE — tenant-psa-reconciler sets enforce=privileged/baseline CLUSTER-WIDE from the global allowHostPortsServer/Worker toggle (NOT per-tenant), so the suite now reads GET /admin/system-settings and expects privileged when either is on, baseline when both off (staging has both ON → privileged, asserted GREEN); (2) compose waits are now env-overridable + generous (COMPOSE_API_WAIT=180 / COMPOSE_WEB_WAIT=300). Residual flake is 100% ENVIRONMENTAL, not a suite/product bug: on a multi-node cluster the compose depends_on init image `busybox:1.37-musl` may be COLD on whichever node the web pod lands on → a docker.io-throttled pull stalls it in Init:0/1 (phase Pending → wait_pod_running times out; RESTARTS=0 = still pulling). Warm nodes stabilise in <45s. Classified `manual` to match phase-1 — run by hand against a warm cluster (or after one warm-up run seeds busybox on all nodes); DISRUPTIVE seed-chain, not CI-gated.
suite         integration-mtls-e2e.sh                    # ADR-054 mTLS edge enforcement E2E, wired into integration-all PARALLEL. STAGING-VALIDATED 17/17 on v2026.7.1-rc.1 (2026-07-01). tenant->provision->nginx-php deployment->domain(auto-route)->provider->bind(PATCH /ingress-routes/:rid/mtls)->cert lifecycle. Asserts via curl: no-cert=handshake-reject (TLSOption), valid=passes gate (distinguishes upstream-403 from gate-403 by the `mtls:` body), revoke=gate-403, per-cert, + CRL verifies (crl.pem WAF 920440 exemption). Slow (~8-12min: deployment provisioning + LE cert), comparable to monitoring-slo. Needs HTTPS_TEST_DOMAIN_BASE (staging wildcard); integration-all provides INTEGRATION_TOKEN+ADMIN_HOST.
manual        integration-platform-domain-rename-e2e.sh  # R16 platform-apex rename E2E (moves platform-ingress IngressRoute Host + cert dnsNames + admin/tenant/webmail/mail settings; ingress_base_domain stays = the decouple). DEV-VALIDATED 12/0 2026-07-02 (rename→verify→revert; trap restore EXIT so a mid-run failure still reverts; apex confirmed restored). Disruptive to the shared apex mid-run → run isolated/by-hand, not in a PARALLEL group. Needs RENAME_TARGET set (else read-only); step 5 cert-serving needs the target to resolve (DEV wildcard covers it).
manual        integration-sftp-gateway-e2e.sh            # ON-NODE (port-forwards platform-api + sftp-gateway to the NODE's localhost) — CANNOT join workstation integration-all (the forward binds the node, not the workstation). THREE fixes 2026-07-02: (1) API_PORT/SFTP_PORT parameterized (defaults 3000/2222); (2) explicit provision trigger POST /admin/tenants/:id/provision (tenant-create is NO auto-provision, routes.ts:205) — the tenant used to just create+wait forever; (3) explicit POST /tenants/:id/files/start to bring up the file-manager (a 0<->1 on-demand Deployment; bare tenant=0 replicas) + fixed a latent false-pass in the FM Ready check. DEV-VALIDATED GREEN 19/19 2026-07-02 (7 SFTP tests: password x4, ssh-key, cross-UID mode-600 read, subdir write, chroot containment). Getting there surfaced a real DEV drift (NOT the script): internal-secret diverged between platform ns (platform-api) and platform-system ns (sftp-gateway) — the two platform-secrets copies weren't kept in sync (bootstrap mirrored ONCE, skip-if-exists), so every SFTP auth 403'd. Fixed live (reconcile + gateway pod-delete) AND drift-proofed: platform-api boot reconcilePlatformSecretsMirror + gateway Reloader annotation + bootstrap now reconciles.

# ── Retired 2026-06-30 (deleted; bit-rotted or superseded) ────────────────
# integration-mail-ha-e2e.sh                  7/13 dead: blob-store removed (ADR-046), node-selector gone, cron */2->*/30
# integration-backups-ui-phase-2026-05-24.sh  dated point-in-time script, superseded by integration-backups-ui.sh
# integration-tenant-bundles-jmap.sh          strict subset of integration-tenant-bundles-jmap-full-e2e.sh
# ── Retired 2026-07-02 ────────────────────────────────────────────────────
# integration-postgres-snapshot-restore.sh    unique WITH_WAL recoveryTargetTime branch (+ chip-persistence & plugin-sidecar assertions) folded into integration-postgres-pitr.sh (WITH_WAL=1 mode); pitr is now a strict superset
